# 公開・収益化に向けた進捗とToDo

最終更新：2026-07-21
現在の状態：**サイトはVercelにデプロイ済み・一般公開済み（URL: https://kentei-app-silk.vercel.app）。Vercel Analytics導入済み。Google AdSenseの申請中で中断（支払い先情報の入力まで完了、次はサイトへの確認コード設置）。**

## ⏭️ 次にやること（再開時はここから）

1. Google AdSense（https://www.google.com/adsense/）に再ログインし、申請の続きから再開する
   - 支払い先情報（住所・電話番号）の入力までは完了済み
   - 次のステップ：サイトにAdSenseの確認コードを設置する案内が出るはず（この作業はClaude Codeに依頼できます）
2. コード設置後、Googleによる**審査**が始まる（数日〜数週間かかる場合あり）
3. 審査**承認後**：発行されたコードを実際に埋め込む、`public/ads.txt`を作成する（詳細は下記「🟡 サイト公開後」参照）
4. （任意）独自ドメインを使いたい場合は、Vercelダッシュボードの「Add Domain」から設定する

このファイルは、プロジェクトの状況を後から見返すため、また別セッションでClaude Codeに作業を引き継ぐ際に、これまでの経緯と残タスクをすぐ把握できるようにするための記録です。作業が進んだら、都度このファイルを更新してください（Claude Codeに依頼する場合は「LAUNCH_CHECKLIST.mdを更新して」と伝えれば反映します）。

---

## これまでに完成した機能

### 統計検定（全23種、計算まで実装済み）
パラメトリック11種（t検定各種・ANOVA・ウェルチ・二元配置・反復測定・F検定・ルビーン・シャピロウィルク・ピアソン相関）、ノンパラメトリック12種（マン・ホイットニーU・ウィルコクソン・クラスカルウォリス・カイ二乗系・フィッシャー・符号検定・フリードマン・マクネマー・コクランQ・ケンドール・スピアマン）。すべて`npm run verify`でR・数表の既知値と照合済み。

### 便利機能
- データの貼り付け一括入力（Excel等からのコピー&ペースト対応）
- 効果量の自動表示（Cohen's d・η²・Cramér's V等、解釈ラベル付き）
- 検定選択ガイド（質問に答えると適切な検定を提案するウィザード、`/guide`）
- 結果グラフ（箱ひげ図・散布図・スロープグラフ・棒グラフ、グレースケール配色、タイトル/軸範囲/目盛り間隔/データ名を編集してPNG・SVG保存可能）
- APA形式での結果コピー（ワンクリックでクリップボードへ）

### 多言語対応（日本語／英語）
- ホーム画面（検定選択画面）右上のボタンでのみ切り替え可能（他の画面には切替ボタンを置かない設計、ユーザー指定による）
- 初回はブラウザの言語設定から自動判定、以後はlocalStorageに保存
- 全23検定の解説・検定選択ガイド・UI文言・結果画面の統計用語まですべて翻訳済み
- 言語切替に伴うMathJax数式の二重表示バグを修正済み（`ExplainDetail.tsx`でkeyによる強制再マウント方式）

### 公開・収益化に向けた準備
- `/faq`・`/contact`・`/privacy`・`/terms` の4ページを新設（日英両対応、フッターからリンク）
- プライバシーポリシーはGoogle AdSense掲載を前提とした文言（Cookie利用・オプトアウト方法・広告配信事業者への言及）を含む
- SEO・技術面：favicon、meta description、OGPタグ、robots.txt、言語切替と連動する`<html lang>`属性を追加
- `vercel.json` を追加済み（Vercelにデプロイした際、`/privacy`等への直接アクセスやリロードで404にならないようにするSPAルーティング設定）

### 検証体制
- `npm run verify`：統計計算・APA形式・多言語翻訳の整合性チェック（99項目、すべて一致）
- Playwright E2Eテスト一式（`/private/tmp/.../scratchpad/e2e*.py`にあるが、セッション終了とともに消える一時ファイルのため、再度必要なら作り直しが必要）
- 計算結果の信頼性向上のため、2026-07-22に以下を実施済み：
  - GitHubリポジトリ `romatt721/kentei-app` をPrivate→**Public**に変更（計算ソースコードを含め全体公開）
  - サイトに `/verification`（計算結果の検証について）ページを新設。検算方法・99項目全一致であることを日英両言語で説明し、フッターからリンク
  - GitHub Actions（`.github/workflows/verify.yml`）を追加し、push・PRごとに`npm run verify`を自動実行。READMEにCIバッジを設置
  - `/verification`ページに「サンプルデータで自分でも検証できる」項目（対応なしt検定・カイ二乗独立性検定の2例）と「既知の制限事項」一覧（球面性補正なし、イェーツ補正なし、正規近似の小標本精度、シャピロ・ウィルクの対応範囲、フィッシャー正確検定は2×2のみ等）を追加
  - 反復測定分散分析の解説ページに「球面性が満たされない場合はGreenhouse-Geisser補正を行う」という誤った記述があったのを発見・修正（実装は補正を行っておらず、結果画面の注記とも矛盾していた）。シャピロ・ウィルク検定の解説にRoyston (1995) の出典も追記
- 2026-07-22、「矛盾や誤りを絶対に残さない」という方針のもと、実装コード・解説文（日英）・共通基盤（APA形式・効果量・検定選択ガイド・検算スクリプトの正しさ自体）を全23検定にわたって突き合わせる監査を実施。発見・修正した実際のバグ：
  - **マクネマー検定の実装バグ**：変化ペア数が25以下の正確二項検定のケースでも、結果画面に「χ²値」「自由度1」が表示されていた（実装が分岐を無視してχ²値を常に返していたため）。統計量欄・自由度欄を二項検定の場合は正しく分岐するよう修正
  - **APA形式コピー機能のバグ**：マクネマー検定の正確二項検定を判定する条件式が、存在しない文字列と照合していたため常に不成立となり、正確二項検定の場合でも「χ²(1) = ..., p = ...」という誤ったAPA形式レポート文が生成されていた。判定条件を修正
  - ケンドールのタウの解説文に「中規模以上の標本でのみ正規近似を使う」という誤った記述（実装は常に正規近似）があり修正
  - プライバシーポリシーが「アクセス解析ツールは使用していない」としていたが、実際はVercel Analyticsを導入済みだった矛盾を修正し、正確な説明に差し替え（最終更新日も更新）
  - クレジット表示ページにVercel Analyticsの記載漏れがあったため追加
  - 効果量の解釈ラベルに関する注記が、Cohen (1988)が定義していないケンドールの一致係数Wにまで一律で出典を付与していた点を修正
  - READMEに残っていた「お問い合わせ先は仮のプレースホルダー」という古い記述（実際は差し替え済み）を修正
  - 上記すべての修正について`npm run verify`に検算を追加（99項目→107項目、すべて一致）
- 2026-07-22、上記に続き「手をつけていない部分」（グラフ描画・入力値の境界/異常値処理・法的文書）も追加で監査。実際に見つかった問題：
  - **グラフの軸範囲設定に実装バグ**：Y軸（散布図はX軸も）の表示範囲を自由入力できる機能に検証がなく、①最小値≧最大値を指定すると軸が反転・NaN化する、②実データより狭い範囲を指定すると警告なしにバー・箱ひげ・点が消える、という問題があった。特に②は「グラフを都合よく切り取って見せられる」というREADME記載の機能自体が持つ信頼性リスクだったため、不正な範囲指定は自動範囲にフォールバックし、範囲外にデータがある場合は画面に警告文を表示するよう修正。Playwrightで実際の画面表示を確認済み
  - **前回のマクネマー検定修正が英語画面で未翻訳だった**：新しく追加した統計量ラベルと注記が翻訳辞書（`statsLabels.ts`）に登録されておらず、英語表示のはずの結果画面に日本語がそのまま出てしまっていた（Playwrightでの実画面確認中に発覚）。辞書に追記して修正した上で、全23検定の結果文言が確実に英訳されることを検算する仕組みを新設（同種の見落としが再発しないようCIで自動検知）
  - グラフの計算ロジック自体（箱ひげ図の四分位・散布図の回帰式・スロープグラフの対応関係・棒グラフのカテゴリ対応）、入力値の境界・異常値処理（分散0・同順位・分割表の周辺度数0等）、プライバシーポリシー・利用規約・お問い合わせページの内容は、追加監査の結果いずれも問題なし（既に適切に実装・記述されていた）
  - `npm run verify`は107項目→108項目に増加、すべて一致
- 2026-07-22、さらに「サイト内リンクの404チェック」「ガイドウィザード・言語切替の実クリック確認」「アクセシビリティ」「セキュリティ（XSS等）」も追加監査。
  - **アクセシビリティ**：axe-coreで全ページ・入力〜結果までのフローをスキャンし、実際に4種類の問題を発見・修正（修正後は全ページ0件）
    - ヘッダー・フッターの`<nav>`にアクセシブルネームがなく、スクリーンリーダーが2つの「ナビゲーション」を区別できなかった → aria-label追加
    - カテゴリバッジ等の配色がWCAG AAのコントラスト比（4.5:1）を満たしていなかった（secondary（緑）は白背景に対しても約3.3:1しかなく、そもものその色を小さな文字色に使うこと自体が不適切だった）→ 文字色専用の濃い緑`secondaryText`を新設し、バッジの背景も調整
    - 検定選択画面でh1の次にh2を飛ばしてh3が出ていた（見出しレベルの飛び）→ h2に修正
    - 文中リンクがホバー時にしか下線が付かず、色だけで識別されていた → 常時下線に修正
  - **セキュリティ**：`dangerouslySetInnerHTML`・`eval`・`innerHTML`・`document.write`は使用箇所なし。貼り付け機能もReactのstateに文字列として格納されるのみで、XSS等の注入経路は現状なし（クライアント完結型アプリのため元々リスクは低いが、念のため確認）。外部リンク（GitHub・aboutads.info等）は`target="_blank"`に`rel="noopener noreferrer"`を統一
  - **リンク切れ**：サイト内の全リンク（動的な`/explain/:id`含む）は定義済みルートと一致。外部URL（GitHubリポジトリ・デプロイ先）もcurlで200を確認
  - **ガイドウィザード・言語切替**：Playwrightで実際にクリックして動作確認。ウィザードは統計的に妥当な検定を提案し、言語切替もエラーなく即座に反映されることを確認

### バージョン管理
- `git init`済み、最初のコミット作成済み（2026-07-21）
- コミットの作成者名は匿名ハンドル `romatt <romatt@users.noreply.github.com>` に設定済み（実際のGitHubアカウント作成後、本物のno-replyアドレスに変わっても問題なし）
- `.gitignore` に `.claude/`（Claude Codeのローカル権限設定）を追加済み。`node_modules`・`dist`・`.DS_Store`等ももちろん除外済み
- GitHubアカウント `romatt721` を作成、`gh auth login`でCLI認証済み（2026-07-21）
- GitHubリポジトリ `romatt721/kentei-app`（Private）を作成し、コードをpush済み（2026-07-21）

---

## 今後のToDo（優先順）

### 🔴 公開前に必須（あなたの対応が必要）
- [x] お問い合わせページのメールアドレスを実際のものに差し替え（`romatt721@gmail.com`、2026-07-21完了）
- [x] gitリポジトリの初期化・最初のコミット（2026-07-21完了）
- [x] GitHub認証（`gh auth login`）完了（2026-07-21）
- [x] GitHubリポジトリ作成・push完了（`romatt721/kentei-app`, Private、2026-07-21）
- [x] Vercelへのデプロイ・GitHub連携完了（2026-07-21、アカウント`romatt721`）
- [ ] （任意）独自ドメインの取得・設定

### 🟡 サイト公開後
- [ ] Google AdSenseに申し込む（サイトが実際に公開された状態で審査申請）
- [ ] 審査**承認後**：発行されたコードを実際に埋め込む
- [ ] 審査**承認後**：`public/ads.txt` を作成し、発行されたpublisher IDを記載する（承認前は設置不可）
- [ ] EU圏からのアクセスが見込まれる場合：Cookie同意バナー（CMP）の実装を検討する

### 🟢 将来、機能や運営方針を変える場合の注意点
- [ ] 有料プラン・物販・直接決済を始める場合 → 特定商取引法に基づく表記ページが別途必要になる
- [ ] Google Analytics等のアクセス解析ツールを導入する場合 → `src/pages/PrivacyPage.tsx` の「4. アクセス解析ツールについて」を改定する
- [ ] 運営者名を匿名（「本サイト運営者」）から実名・屋号に変更したい場合 → `PrivacyPage.tsx` と `TermsPage.tsx` の該当箇所を変更する

---

## 参考：主要な設計判断の経緯

- 言語切替ボタンをホーム画面のみに限定：全画面に置くと「結果画面のグラフタイトルが古い言語のまま残る」「説明ページ表示中に切替えると数式が二重表示される」といった複雑さがあったため、ユーザーの要望でホーム画面限定に簡略化した
- プライバシーポリシーは「データはブラウザ内で完結し外部送信されない」という計算機能自体の特長と、「広告配信のみ第三者Cookieを使用する場合がある」という開示を両立する構成にしている
- 運営者名・連絡先メールアドレスはユーザー自身で今後差し替える前提の仮の値になっている
